# Repository conventions

## Git workflow
- Commit changes directly to `main` and push to `main` by default.
- Do NOT create a new feature branch for each change.
- Only create a separate branch when the user explicitly asks for one.
