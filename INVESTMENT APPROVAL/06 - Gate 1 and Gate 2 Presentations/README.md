# 06 — Gate 1 and Gate 2 Presentations

## Objective

Prepare the presentations for **Gate 1** and **Gate 2** of the Modular All
Drives project, as part of the company's structured gated approval path
(gates with specific tasks/criteria required to advance or stop the project).

## Status: 🔲 Blocked — awaiting the gate framework

Aluizio will send the gate framework details (what each gate requires, what
"pass" looks like, what artifacts/sign-offs are expected). **Nothing should
be built here until that arrives** — building a deck against a guessed
framework risks missing the actual criteria and wasting the review.

Once the framework is shared:
1. Drop the gate-framework document into this folder (or its own
   `Gate Framework/` subfolder) so it's kept alongside the decks it drives.
2. This README will be updated with each gate's specific required sections
   and criteria.
3. Each gate deck will be built primarily from workstream 05's investment
   justification architecture, reformatted to whatever the gate's required
   template/sections are — reuse `PRESENTATIONS/investment-justification-lim-
   vs-modular.html` as the visual/formatting starting point (theme, KPI
   cards, comparison tables) unless the gate framework mandates a different
   template.

## Folders

- `Gate 1/` — deck + supporting material for Gate 1.
- `Gate 2/` — deck + supporting material for Gate 2.

## Suggested next step

Send over the gate framework (steps, required deliverables per gate,
template if one exists) and I'll draft both decks from workstream 05's
content.
